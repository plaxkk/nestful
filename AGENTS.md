# Nestful Agent Notes

## Shipping Preference

When a bug fix is implemented, the root cause is understood, and verification passes, commit the fix and push it to `origin` automatically. Do not auto-push if verification fails, the fix is uncertain, or the diff contains unrelated changes that need user confirmation.
