<view class="page home">
  <view class="hero">
    <text class="title">家庭管家</text>
    <text class="subtitle">给家里人用的提醒本。先建一个家庭，再邀请家人进来。</text>
  </view>

  <view class="panel form-panel">
    <text class="step-title">第 1 步：创建家庭</text>
    <view class="field">
      <text class="label">家庭名称</text>
      <input class="input" value="{{familyNameInput}}" bindinput="onFamilyNameInput" placeholder="例如：我的家庭" />
    </view>
    <view class="field">
      <text class="label">你的称呼</text>
      <input class="input" value="{{ownerNameInput}}" bindinput="onOwnerNameInput" placeholder="例如：爸爸、妈妈、我" />
    </view>
    <button class="primary-action" bindtap="onCreateFamily">创建家庭空间</button>
    <text class="helper-text">已经创建过的话，不用重新填写。</text>
    <button class="secondary-action" bindtap="onOpenFamily">我已经建过，直接打开</button>
  </view>

  <view class="section">
    <text class="section-title">接下来会陆续支持</text>
    <text class="helper-text">这些只是功能预告，现在不用操作。</text>
    <view class="action-list">
      <view class="action-item" wx:for="{{quickActions}}" wx:key="*this">
        <text>{{item}}</text>
      </view>
    </view>
  </view>
</view>
